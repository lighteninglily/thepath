MAJOR UPDATE: Sermon Builder + Song Editor Fixes

SERMON BUILDER - COMPLETED:
============================
✅ Templates now render beautifully with VisualCanvas
✅ Customize button fully functional - opens Visual Editor
✅ AI template suggestions working
✅ Template gallery with search/filtering
✅ Drag & drop slide reordering
✅ 10 professional templates (Title, Scripture, Point, Multi-Point, Quote, etc.)
✅ Debug logging added for troubleshooting

SONG EDITOR - FIXED:
====================
✅ Backgrounds now persist correctly when reopening editor
✅ Layouts now persist correctly (no more random changes)
✅ Fixed useEffect that was overwriting loaded data
✅ handleOpenSlideEditor now checks for existing data first

KEY FIXES THIS SESSION:
========================
1. Sermon preview rendering (VisualCanvas integration)
2. Customize button wired to SlideDesigner
3. Visual Editor round-trip editing
4. Background persistence (handleOpenSlideEditor fix)
5. Layout persistence (useEffect guard)
6. Comprehensive debug logging

FILES CHANGED:
==============
- src/components/sermon/SermonSlideEditor.tsx (VisualCanvas + logging)
- src/components/sermon/AddSermonModal.tsx (Visual Editor integration)
- src/components/songs/SongFormModal.tsx (Persistence fixes)

WHAT WORKS NOW:
===============
SERMON BUILDER:
- Type content → AI analyzes → Suggests templates
- Click template or "AI Suggest" → Beautiful formatted slide
- Click "Customize" → Opens full Visual Editor
- Edit fonts, colors, positions → Save → Back to builder
- Save sermon → Ready for service

SONG EDITOR:
- Add background → Save → Reopen → Background STAYS ✓
- Change layout → Save → Reopen → Layout STAYS ✓
- No more random layout changes ✓

STILL TO TEST:
==============
- Sermon builder with actual content
- Template rendering (check console logs)
- Visual Editor customization round-trip
- Save sermon to service

STATUS: Ready for testing!
